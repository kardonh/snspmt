# SNS PMT 전체 기능 목록

## 📋 목차
1. [사용자 인증 및 관리](#1-사용자-인증-및-관리)
2. [포인트 시스템](#2-포인트-시스템)
3. [주문 관리](#3-주문-관리)
4. [추천인 시스템](#4-추천인-시스템)
5. [쿠폰 시스템](#5-쿠폰-시스템)
6. [관리자 기능](#6-관리자-기능)
7. [서비스 관리 (카탈로그)](#7-서비스-관리-카탈로그)
8. [블로그 시스템](#8-블로그-시스템)
9. [공지사항](#9-공지사항)
10. [SMM Panel 통합](#10-smm-panel-통합)
11. [스케줄러 및 자동화](#11-스케줄러-및-자동화)
12. [프론트엔드 페이지](#12-프론트엔드-페이지)

---

## 1. 사용자 인증 및 관리

### 1.1 회원가입
- **엔드포인트**: `POST /api/register`
- **기능**: 새 사용자 등록, 자동 지갑 생성
- **요청**: `{ "user_id", "email", "username" }`
- **응답**: 사용자 정보 및 초기 포인트

### 1.2 사용자 정보 조회
- **엔드포인트**: `GET /api/users/<user_id>`
- **기능**: 사용자 정보 조회 (없으면 자동 생성)
- **응답**: `{ "user_id", "username", "email", "points" }`

### 1.3 Google 로그인
- **엔드포인트**: `POST /api/auth/google-login`
- **기능**: Google OAuth 로그인 처리
- **요청**: `{ "id_token" }`
- **응답**: 사용자 정보

### 1.4 Kakao 로그인
- **엔드포인트**: `POST /api/auth/kakao-login`
- **기능**: Kakao OAuth 로그인 처리
- **요청**: `{ "access_token", "id_token" }`
- **응답**: 사용자 정보

### 1.5 Kakao 토큰 교환
- **엔드포인트**: `POST /api/auth/kakao-token`
- **기능**: Kakao 인증 코드를 토큰으로 교환

### 1.6 Google 콜백
- **엔드포인트**: `GET /api/auth/google-callback`
- **기능**: Google OAuth 콜백 처리

### 1.7 일반 로그인
- **엔드포인트**: `POST /api/auth/login`
- **기능**: 이메일/비밀번호 로그인
- **요청**: `{ "email", "password" }`

---

## 2. 포인트 시스템

### 2.1 포인트 조회
- **엔드포인트**: `GET /api/points?user_id=<user_id>`
- **기능**: 사용자 지갑 잔액 조회
- **응답**: `{ "points": 1000, "wallet_balance": 1000, "user_id": "..." }`

### 2.2 포인트 구매 신청 (수동)
- **엔드포인트**: `POST /api/points/purchase`
- **기능**: 포인트 구매 신청 (관리자 승인 필요)
- **요청**: `{ "user_id", "amount", "price", "buyer_name", "bank_info" }`
- **응답**: `{ "success": true, "purchase_id": 123 }`

### 2.3 KCP 결제 시스템
#### 2.3.1 거래 등록
- **엔드포인트**: `POST /api/points/purchase-kcp/register`
- **기능**: KCP 표준결제 거래 등록
- **요청**: `{ "user_id", "amount", "price", "good_name", "pay_method" }`
- **응답**: `{ "ordr_idxx", "approval_key", "pay_url" }`

#### 2.3.2 결제창 폼 데이터
- **엔드포인트**: `POST /api/points/purchase-kcp/payment-form`
- **기능**: 결제창 호출을 위한 폼 데이터 생성

#### 2.3.3 결제 인증 결과
- **엔드포인트**: `POST /api/points/purchase-kcp/return`
- **기능**: KCP에서 전달받은 인증결과 처리

#### 2.3.4 결제 승인
- **엔드포인트**: `POST /api/points/purchase-kcp/approve`
- **기능**: 실제 결제 승인 요청
- **요청**: `{ "ordr_idxx", "enc_data", "enc_info", "tran_cd" }`

### 2.4 포인트 차감
- **엔드포인트**: `POST /api/points/deduct`
- **기능**: 주문 시 포인트 차감
- **요청**: `{ "user_id", "amount", "description" }`

### 2.5 포인트 구매 내역
- **엔드포인트**: `GET /api/points/purchase-history?user_id=<user_id>`
- **기능**: 사용자의 포인트 구매 내역 조회
- **응답**: 구매 내역 목록 (날짜, 금액, 상태)

---

## 3. 주문 관리

### 3.1 주문 생성
- **엔드포인트**: `POST /api/orders`
- **기능**: 새 주문 생성 (단일/패키지/예약/분할)
- **주문 유형**:
  - **단일 주문**: 즉시 SMM Panel API 호출
  - **패키지 주문**: 여러 단계로 나누어 처리
  - **예약 주문**: 지정된 시간에 처리
  - **분할 주문**: 여러 날에 나누어 발송
- **요청**:
  ```json
  {
    "user_id": "string",
    "order_type": "single|package|scheduled|split",
    "items": [
      {
        "variant_id": 1,
        "link": "https://...",
        "quantity": 100,
        "unit_price": 1000
      }
    ],
    "scheduled_at": "2025-11-20T10:00:00" (예약 주문 시),
    "split_days": 7 (분할 주문 시),
    "coupon_code": "COUPON123" (선택)
  }
  ```
- **처리 과정**:
  1. 포인트 잔액 확인
  2. 쿠폰 적용 (있는 경우)
  3. 포인트 차감
  4. 주문 생성
  5. SMM Panel API 호출 (단일 주문)
  6. work_jobs 생성 (패키지/예약 주문)
  7. 추천인 커미션 계산 및 기록

### 3.2 주문 목록 조회
- **엔드포인트**: `GET /api/orders?user_id=<user_id>`
- **기능**: 사용자의 주문 목록 조회
- **응답**: 주문 목록 (상태, 금액, 생성일, SMM Panel 주문 ID 등)

### 3.3 주문 상태 확인
- **엔드포인트**: `POST /api/orders/check-status`
- **기능**: SMM Panel에서 주문 상태 확인
- **요청**: `{ "order_id" }`

### 3.4 주문 상태 업데이트
- **엔드포인트**: `PUT /api/orders/<order_id>/status`
- **기능**: 주문 상태 수동 업데이트
- **요청**: `{ "status": "completed|processing|cancelled" }`

### 3.5 패키지 주문 처리 시작
- **엔드포인트**: `POST /api/orders/start-package-processing`
- **기능**: 패키지 주문의 첫 번째 단계 시작
- **요청**: `{ "order_id" }`

### 3.6 패키지 진행 상황 조회
- **엔드포인트**: `GET /api/orders/<order_id>/package-progress`
- **기능**: 패키지 주문의 진행 상황 조회
- **응답**: 현재 단계, 완료된 단계 수, 다음 단계 예정 시간

### 3.7 예약 주문 생성
- **엔드포인트**: `POST /api/scheduled-orders`
- **기능**: 예약된 주문 생성
- **요청**: `{ "user_id", "service_id", "link", "quantity", "price", "scheduled_at" }`

### 3.8 예약 주문 목록 (관리자)
- **엔드포인트**: `GET /api/admin/scheduled-orders`
- **기능**: 모든 예약 주문 목록 조회
- **인증**: 관리자 토큰 필요

---

## 4. 추천인 시스템

### 4.1 내 추천인 코드 조회
- **엔드포인트**: `GET /api/referral/my-codes?user_id=<user_id>`
- **기능**: 사용자의 추천인 코드 및 통계 조회
- **응답**: 
  ```json
  {
    "referral_code": "REF12345678",
    "total_commission": 50000,
    "usage_count": 10
  }
  ```

### 4.2 추천인 코드 사용
- **엔드포인트**: `POST /api/referral/use-code`
- **기능**: 추천인 코드 사용 (쿠폰 발급)
- **요청**: `{ "user_id", "referral_code" }`
- **처리**:
  1. 추천인 코드 유효성 확인
  2. referrals 테이블에 기록
  3. 쿠폰 발급 (특정 상품용, 1회 사용)
  4. 추천인에게 커미션 기록 (10%)

### 4.3 추천인 코드 검증
- **엔드포인트**: `GET /api/referral/validate-code?code=<code>`
- **기능**: 추천인 코드 유효성 검증
- **응답**: `{ "valid": true/false }`

### 4.4 커미션 조회
- **엔드포인트**: `GET /api/referral/commissions?user_id=<user_id>`
- **기능**: 사용자의 커미션 내역 조회
- **응답**: 커미션 목록 (상태: accrued/void/paid_out, 금액, 생성일)

### 4.5 커미션 포인트 조회
- **엔드포인트**: `GET /api/referral/commission-points?user_id=<user_id>`
- **기능**: 미지급 커미션 포인트 합계 조회
- **응답**: `{ "total_commission": 50000 }`

### 4.6 커미션 거래 내역
- **엔드포인트**: `GET /api/referral/commission-transactions?user_id=<user_id>`
- **기능**: 커미션 거래 내역 상세 조회

### 4.7 추천인 통계
- **엔드포인트**: `GET /api/referral/stats?user_id=<user_id>`
- **기능**: 추천인 관련 통계 조회
- **응답**: 총 추천인 수, 총 커미션 등

### 4.8 추천인 목록
- **엔드포인트**: `GET /api/referral/referrals?user_id=<user_id>`
- **기능**: 내가 추천한 사용자 목록 조회

### 4.9 출금 요청
- **엔드포인트**: `POST /api/referral/withdrawal-request`
- **기능**: 커미션 출금 요청
- **요청**: 
  ```json
  {
    "user_id": "string",
    "amount": 10000,
    "bank_name": "string",
    "account_number": "string",
    "account_holder": "string"
  }
  ```

### 4.10 쿠폰 발급 (추천인)
- **엔드포인트**: `POST /api/referral/issue-coupon`
- **기능**: 추천인 코드 사용 시 쿠폰 자동 발급
- **요청**: `{ "user_id", "referral_code" }`

---

## 5. 쿠폰 시스템

### 5.1 사용자 쿠폰 조회
- **엔드포인트**: `GET /api/user/coupons?user_id=<user_id>`
- **기능**: 사용 가능한 쿠폰 목록 조회
- **응답**: 
  ```json
  [
    {
      "user_coupon_id": 1,
      "coupon_code": "COUPON123",
      "coupon_name": "10% 할인",
      "discount_type": "percentage",
      "discount_value": 10,
      "status": "active",
      "issued_at": "2025-01-01",
      "used_at": null,
      "valid_until": "2025-12-31"
    }
  ]
  ```

### 5.2 쿠폰 사용
- **기능**: 주문 생성 시 쿠폰 코드 입력하여 할인 적용
- **처리**: 주문 생성 API에서 자동 처리

---

## 6. 관리자 기능

### 6.1 관리자 통계
- **엔드포인트**: `GET /api/admin/stats`
- **기능**: 전체 통계 조회
- **인증**: `X-Admin-Token` 헤더 필요
- **응답**: 
  ```json
  {
    "total_users": 1000,
    "total_orders": 5000,
    "total_revenue": 10000000,
    "pending_purchases": 5,
    "today_orders": 50,
    "today_revenue": 500000
  }
  ```

### 6.2 사용자 관리
- **엔드포인트**: `GET /api/admin/users`
- **기능**: 전체 사용자 목록 조회
- **쿼리 파라미터**: `page`, `limit`, `search`
- **응답**: 사용자 목록 (이메일, 가입일, 포인트 등)

### 6.3 주문 관리
- **엔드포인트**: `GET /api/admin/transactions`
- **기능**: 전체 거래 내역 조회
- **응답**: 주문 목록 (사용자, 금액, 상태, 생성일)

### 6.4 포인트 구매 신청 관리
- **조회**: `GET /api/admin/purchases`
- **수정**: `PUT /api/admin/purchases/<purchase_id>`
- **기능**: 포인트 구매 신청 승인/거절
- **요청**: `{ "status": "approved|rejected" }`
- **처리**: 승인 시 지갑에 포인트 자동 충전

### 6.5 추천인 관리
#### 6.5.1 추천인 코드 생성
- **엔드포인트**: `POST /api/admin/referral/register`
- **기능**: 관리자용 추천인 코드 생성
- **요청**: `{ "user_email", "name", "phone" }`

#### 6.5.2 추천인 코드 목록
- **엔드포인트**: `GET /api/admin/referral/codes`
- **기능**: 전체 추천인 코드 목록 조회

#### 6.5.3 추천인 목록
- **엔드포인트**: `GET /api/admin/referral/list`
- **기능**: 전체 추천인 목록 조회

#### 6.5.4 커미션 개요
- **엔드포인트**: `GET /api/admin/referral/commission-overview`
- **기능**: 전체 커미션 개요 조회

#### 6.5.5 커미션 지급
- **엔드포인트**: `POST /api/admin/referral/pay-commission`
- **기능**: 커미션 수동 지급
- **요청**: `{ "referrer_id", "amount", "payment_method" }`

#### 6.5.6 커미션 지급 내역
- **엔드포인트**: `GET /api/admin/referral/payment-history`
- **기능**: 커미션 지급 내역 조회

#### 6.5.7 커미션 내역
- **엔드포인트**: `GET /api/admin/referral/commissions`
- **기능**: 전체 커미션 내역 조회

#### 6.5.8 모든 추천인 코드 활성화
- **엔드포인트**: `POST /api/admin/referral/activate-all`
- **기능**: 모든 추천인 코드 일괄 활성화

### 6.6 출금 요청 관리
#### 6.6.1 출금 요청 목록
- **엔드포인트**: `GET /api/admin/withdrawal-requests`
- **기능**: 출금 요청 목록 조회

#### 6.6.2 출금 처리
- **엔드포인트**: `POST /api/admin/process-withdrawal`
- **기능**: 출금 요청 처리 (승인/거절)
- **요청**: `{ "request_id", "status": "approved|rejected" }`

### 6.7 패키지 주문 재처리
- **엔드포인트**: `POST /api/admin/reprocess-package-orders`
- **기능**: 멈춰있는 패키지 주문 재처리

### 6.8 공지사항 관리
#### 6.8.1 공지사항 목록
- **엔드포인트**: `GET /api/admin/notices`
- **기능**: 전체 공지사항 목록 조회

#### 6.8.2 공지사항 생성
- **엔드포인트**: `POST /api/admin/notices`
- **기능**: 공지사항 생성
- **요청**: `{ "title", "body", "image_url", "is_active" }`

#### 6.8.3 공지사항 수정
- **엔드포인트**: `PUT /api/admin/notices/<notice_id>`
- **기능**: 공지사항 수정

#### 6.8.4 공지사항 삭제
- **엔드포인트**: `DELETE /api/admin/notices/<notice_id>`
- **기능**: 공지사항 삭제

### 6.9 이미지 업로드
- **엔드포인트**: `POST /api/admin/upload-image`
- **기능**: 관리자용 이미지 업로드
- **Content-Type**: `multipart/form-data`
- **응답**: `{ "image_url": "/static/uploads/..." }`

### 6.10 데이터베이스 마이그레이션
- **엔드포인트**: `POST /api/admin/migrate-database`
- **기능**: 데이터베이스 마이그레이션 실행
- **인증**: 관리자 토큰 필요

---

## 7. 서비스 관리 (카탈로그)

### 7.1 카테고리 관리
#### 7.1.1 카테고리 목록 조회
- **엔드포인트**: `GET /api/admin/categories?include_inactive=true`
- **기능**: 카테고리 목록 조회
- **응답**: `{ "categories": [...], "count": 10 }`

#### 7.1.2 카테고리 생성
- **엔드포인트**: `POST /api/admin/categories`
- **기능**: 새 카테고리 생성
- **요청**: `{ "name", "slug", "image_url", "is_active" }`

#### 7.1.3 카테고리 상세 조회
- **엔드포인트**: `GET /api/admin/categories/<category_id>`
- **기능**: 카테고리 상세 정보 조회

#### 7.1.4 카테고리 수정
- **엔드포인트**: `PUT /api/admin/categories/<category_id>`
- **기능**: 카테고리 정보 수정

#### 7.1.5 카테고리 비활성화
- **엔드포인트**: `DELETE /api/admin/categories/<category_id>`
- **기능**: 카테고리 비활성화 (실제 삭제는 하지 않음)

### 7.2 상품 관리
#### 7.2.1 상품 목록 조회
- **엔드포인트**: `GET /api/admin/products?category_id=1`
- **기능**: 상품 목록 조회 (카테고리별 필터링 가능)
- **응답**: `{ "products": [...], "count": 20 }`

#### 7.2.2 상품 생성
- **엔드포인트**: `POST /api/admin/products`
- **기능**: 새 상품 생성
- **요청**: `{ "category_id", "name", "description", "is_domestic", "auto_tag" }`

#### 7.2.3 상품 상세 조회
- **엔드포인트**: `GET /api/admin/products/<product_id>`
- **기능**: 상품 상세 정보 조회

#### 7.2.4 상품 수정
- **엔드포인트**: `PUT /api/admin/products/<product_id>`
- **기능**: 상품 정보 수정

#### 7.2.5 상품 삭제
- **엔드포인트**: `DELETE /api/admin/products/<product_id>`
- **기능**: 상품 삭제 (옵션이 있으면 오류)

### 7.3 상품 옵션 (Variants) 관리
#### 7.3.1 옵션 목록 조회
- **엔드포인트**: `GET /api/admin/product-variants?product_id=1`
- **기능**: 상품 옵션 목록 조회
- **응답**: `{ "variants": [...], "count": 5 }`

#### 7.3.2 옵션 생성
- **엔드포인트**: `POST /api/admin/product-variants`
- **기능**: 새 상품 옵션 생성
- **요청**: 
  ```json
  {
    "product_id": 1,
    "name": "Base Package",
    "price": 10.50,
    "min_quantity": 100,
    "max_quantity": 10000,
    "delivery_time_days": 2,
    "is_active": true,
    "meta_json": {
      "smm_service_id": 12345,
      "panel": "providerA"
    },
    "api_endpoint": "https://provider/api"
  }
  ```

#### 7.3.3 옵션 상세 조회
- **엔드포인트**: `GET /api/admin/product-variants/<variant_id>`
- **기능**: 옵션 상세 정보 조회

#### 7.3.4 옵션 수정
- **엔드포인트**: `PUT /api/admin/product-variants/<variant_id>`
- **기능**: 옵션 정보 수정

#### 7.3.5 옵션 삭제
- **엔드포인트**: `DELETE /api/admin/product-variants/<variant_id>`
- **기능**: 옵션 삭제

### 7.4 패키지 관리
#### 7.4.1 패키지 목록 조회
- **엔드포인트**: `GET /api/admin/packages`
- **기능**: 패키지 목록 조회 (items 포함)
- **응답**: `{ "packages": [...], "count": 5 }`

#### 7.4.2 패키지 생성
- **엔드포인트**: `POST /api/admin/packages`
- **기능**: 새 패키지 생성
- **요청**: 
  ```json
  {
    "category_id": 1,
    "name": "Starter Pack",
    "description": "30-day growth",
    "items": [
      {
        "variant_id": 10,
        "step": 1,
        "term_value": 1,
        "term_unit": "day",
        "quantity": 100,
        "repeat_count": 30
      }
    ]
  }
  ```

#### 7.4.3 패키지 상세 조회
- **엔드포인트**: `GET /api/admin/packages/<package_id>`
- **기능**: 패키지 상세 정보 조회 (items 포함)

#### 7.4.4 패키지 수정
- **엔드포인트**: `PUT /api/admin/packages/<package_id>`
- **기능**: 패키지 정보 수정 (items 배열 전달 시 전체 교체)

#### 7.4.5 패키지 삭제
- **엔드포인트**: `DELETE /api/admin/packages/<package_id>`
- **기능**: 패키지 삭제

---

## 8. 블로그 시스템

### 8.1 블로그 포스트 목록
- **엔드포인트**: `GET /api/blog/posts`
- **기능**: 블로그 포스트 목록 조회
- **쿼리 파라미터**: `page`, `limit`, `category`, `tag`

### 8.2 블로그 포스트 상세
- **엔드포인트**: `GET /api/blog/posts/<post_id>`
- **기능**: 블로그 포스트 상세 조회

### 8.3 블로그 카테고리 목록
- **엔드포인트**: `GET /api/blog/categories`
- **기능**: 블로그 카테고리 목록 조회

### 8.4 블로그 태그 목록
- **엔드포인트**: `GET /api/blog/tags`
- **기능**: 블로그 태그 목록 조회

### 8.5 블로그 포스트 생성 (관리자)
- **엔드포인트**: `POST /api/blog/posts`
- **기능**: 블로그 포스트 생성
- **인증**: 관리자 토큰 필요
- **요청**: `{ "title", "content", "category", "tags" }`

### 8.6 블로그 포스트 수정 (관리자)
- **엔드포인트**: `PUT /api/blog/posts/<post_id>`
- **기능**: 블로그 포스트 수정
- **인증**: 관리자 토큰 필요

### 8.7 블로그 포스트 삭제 (관리자)
- **엔드포인트**: `DELETE /api/blog/posts/<post_id>`
- **기능**: 블로그 포스트 삭제
- **인증**: 관리자 토큰 필요

---

## 9. 공지사항

### 9.1 활성 공지사항 조회
- **엔드포인트**: `GET /api/notices/active`
- **기능**: 활성화된 공지사항 목록 조회
- **응답**: 공지사항 목록 (제목, 내용, 이미지, 링크)

---

## 10. SMM Panel 통합

### 10.1 SMM Panel 서비스 목록 조회
- **엔드포인트**: `GET /api/smm-panel/services`
- **기능**: SMM Panel에서 제공하는 서비스 목록 조회
- **응답**: `{ "success": true, "services": [...], "service_ids": [...] }`

### 10.2 SMM Panel API 호출
- **엔드포인트**: `POST /api/smm-panel`
- **기능**: SMM Panel API 프록시
- **요청**: 
  ```json
  {
    "action": "services|balance|add|status|refill|cancel",
    "service": "string",
    "link": "string",
    "quantity": 100,
    "comments": "string"
  }
  ```

### 10.3 SMM Panel 테스트
- **엔드포인트**: `GET /api/smm-panel/test`
- **기능**: SMM Panel API 연결 테스트

---

## 11. 스케줄러 및 자동화

### 11.1 예약 주문 처리
- **엔드포인트**: `POST /api/cron/process-scheduled-orders`
- **기능**: 예약된 주문들을 처리하는 Cron 작업
- **처리**: 
  - 예약 시간이 된 주문 찾기
  - SMM Panel API 호출
  - 주문 상태 업데이트

### 11.2 분할 발송 처리
- **엔드포인트**: `POST /api/cron/process-split-deliveries`
- **기능**: 분할 발송 주문들을 처리하는 Cron 작업
- **처리**: 
  - 분할 발송 일정 확인
  - 해당 날짜의 발송량 계산
  - SMM Panel API 호출

### 11.3 SMM Panel 상태 확인 스케줄러
- **기능**: 5분마다 주문 상태 확인
- **처리**: 
  - 진행 중인 주문 조회
  - SMM Panel에서 상태 확인
  - 주문 상태 업데이트
  - 패키지 주문의 다음 단계 처리

### 11.4 백그라운드 스케줄러
- **기능**: 주기적으로 예약 주문 및 패키지 주문 처리
- **처리**: 
  - 예약 주문 처리
  - 패키지 주문의 다음 단계 시작
  - 상태 업데이트

---

## 12. 프론트엔드 페이지

### 12.1 홈 페이지 (`/`)
- **파일**: `src/pages/Home.jsx`
- **기능**:
  - 서비스 카테고리 표시
  - 상품 목록 표시
  - 주문 생성 (단일/패키지/예약/분할)
  - 쿠폰 적용
  - SMM Panel 서비스 필터링

### 12.2 포인트 페이지 (`/points`)
- **파일**: `src/pages/PointsPage.jsx`
- **기능**:
  - 포인트 잔액 조회
  - 포인트 구매 (KCP 결제)
  - 포인트 구매 내역 조회

### 12.3 주문 내역 페이지 (`/orders`)
- **파일**: `src/pages/OrdersPage.jsx`
- **기능**:
  - 주문 목록 조회
  - 주문 상세 정보
  - 주문 상태 확인
  - 패키지 주문 진행 상황

### 12.4 결제 페이지 (`/payment/:platform`)
- **파일**: `src/pages/PaymentPage.jsx`
- **기능**:
  - 주문 요약
  - 결제 처리
  - 포인트 차감

### 12.5 주문 완료 페이지 (`/order-complete/:orderId`)
- **파일**: `src/pages/OrderCompletePage.jsx`
- **기능**:
  - 주문 완료 확인
  - 주문 정보 표시

### 12.6 추천인 대시보드 (`/referral`)
- **파일**: `src/pages/ReferralDashboard.jsx`
- **기능**:
  - 내 추천인 코드 조회
  - 추천인 통계
  - 커미션 내역
  - 출금 요청

### 12.7 설정 페이지 (`/settings`)
- **파일**: `src/pages/SettingsPage.jsx`
- **기능**:
  - 사용자 정보 수정
  - 로그아웃

### 12.8 FAQ 페이지 (`/faq`)
- **파일**: `src/pages/FAQPage.jsx`
- **기능**: 자주 묻는 질문

### 12.9 블로그 페이지 (`/blog`)
- **파일**: `src/pages/BlogPage.jsx`
- **기능**:
  - 블로그 포스트 목록
  - 카테고리/태그 필터링

### 12.10 블로그 상세 페이지 (`/blog/:id`)
- **파일**: `src/pages/BlogDetailPage.jsx`
- **기능**: 블로그 포스트 상세 보기

### 12.11 관리자 대시보드 (`/admin`)
- **파일**: `src/pages/AdminPage.jsx`
- **기능**:
  - **대시보드**: 전체 통계 (사용자, 주문, 매출)
  - **사용자 관리**: 사용자 목록, 검색
  - **주문 관리**: 전체 주문 목록, 상태 관리
  - **포인트 구매 신청**: 구매 신청 승인/거절
  - **추천인 관리**: 추천인 코드, 커미션 관리
  - **블로그 관리**: 블로그 포스트 관리
  - **서비스 관리**: 카테고리/상품/옵션/패키지 관리
  - **공지사항 관리**: 공지사항 CRUD
  - **커미션 관리**: 커미션 지급 내역

### 12.12 관리자 블로그 페이지 (`/admin/blog`)
- **파일**: `src/pages/AdminBlogPage.jsx`
- **기능**: 블로그 포스트 작성 및 관리

### 12.13 카카오 콜백 (`/kakao-callback`)
- **파일**: `src/pages/KakaoCallback.jsx`
- **기능**: Kakao OAuth 콜백 처리

---

## 13. 공통 컴포넌트

### 13.1 Layout
- **파일**: `src/components/Layout.jsx`
- **기능**: 전체 레이아웃 (헤더, 사이드바, 하단 탭바)

### 13.2 Header
- **파일**: `src/components/Header.jsx`
- **기능**: 상단 헤더 (로고, 메뉴)

### 13.3 Sidebar
- **파일**: `src/components/Sidebar.jsx`
- **기능**: 사이드 메뉴 (포인트, 주문, 추천인 등)

### 13.4 StatusBar
- **파일**: `src/components/StatusBar.jsx`
- **기능**: 포인트 잔액 표시

### 13.5 BottomTabBar
- **파일**: `src/components/BottomTabBar.jsx`
- **기능**: 하단 탭바 (홈, 주문, 포인트, 설정)

### 13.6 AuthModal
- **파일**: `src/components/AuthModal.jsx`
- **기능**: 로그인/회원가입 모달

### 13.7 LoginModal
- **파일**: `src/components/LoginModal.jsx`
- **기능**: 로그인 모달

### 13.8 NoticePopup
- **파일**: `src/components/NoticePopup.jsx`
- **기능**: 공지사항 팝업

### 13.9 ReferralRegistration
- **파일**: `src/components/ReferralRegistration.jsx`
- **기능**: 추천인 등록 모달

### 13.10 AdminServiceManagement
- **파일**: `src/components/AdminServiceManagement.jsx`
- **기능**: 서비스 관리 UI (카테고리/상품/옵션 관리)

### 13.11 ErrorBoundary
- **파일**: `src/components/ErrorBoundary.jsx`
- **기능**: 에러 처리

### 13.12 LoadingSpinner
- **파일**: `src/components/LoadingSpinner.jsx`
- **기능**: 로딩 스피너

### 13.13 ProtectedRoute
- **파일**: `src/components/ProtectedRoute.jsx`
- **기능**: 인증이 필요한 라우트 보호

---

## 14. Context (상태 관리)

### 14.1 AuthContext
- **파일**: `src/contexts/AuthContext.jsx`
- **기능**: 사용자 인증 상태 관리

### 14.2 LanguageContext
- **파일**: `src/contexts/LanguageContext.jsx`
- **기능**: 다국어 지원

### 14.3 GuestContext
- **파일**: `src/contexts/GuestContext.jsx`
- **기능**: 게스트 모드 관리

### 14.4 NoticeContext
- **파일**: `src/contexts/NoticeContext.jsx`
- **기능**: 공지사항 상태 관리

---

## 15. 유틸리티

### 15.1 API 유틸리티
- **파일**: `src/utils/api.js`
- **기능**: API 호출 헬퍼 함수

### 15.2 추천인 스토리지
- **파일**: `src/utils/referralStorage.js`
- **기능**: 추천인 데이터 로컬 스토리지 관리

### 15.3 로거
- **파일**: `src/utils/logger.js`
- **기능**: 로깅 유틸리티

---

## 16. 데이터베이스 스키마

### 16.1 주요 테이블
- **users**: 사용자 정보 (user_id, external_uid, email, username)
- **wallets**: 지갑 정보 (wallet_id, user_id, balance)
- **wallet_transactions**: 지갑 거래 내역 (type: topup/order_debit/commission_credit)
- **categories**: 카테고리 (category_id, name, slug, is_active)
- **products**: 상품 (product_id, category_id, name, description)
- **product_variants**: 상품 옵션 (variant_id, product_id, name, price, meta_json)
- **packages**: 패키지 (package_id, category_id, name, description)
- **package_items**: 패키지 아이템 (package_item_id, package_id, variant_id, step)
- **orders**: 주문 (order_id, user_id, status, total_amount)
- **order_items**: 주문 아이템 (order_item_id, order_id, variant_id, quantity)
- **work_jobs**: 작업 큐 (job_id, order_id, job_type, status, payload_json)
- **referrals**: 추천인 (referral_id, referrer_id, referred_user_id)
- **commissions**: 커미션 (commission_id, referrer_id, order_id, amount, status)
- **payout_requests**: 출금 요청 (request_id, user_id, amount, status)
- **payouts**: 출금 내역 (payout_id, request_id, amount, status)
- **coupons**: 쿠폰 (coupon_id, name, discount_type, discount_value)
- **user_coupons**: 사용자 쿠폰 (user_coupon_id, user_id, coupon_id, status)
- **notices**: 공지사항 (notice_id, title, body, image_url)
- **blogs**: 블로그 (blog_id, title, content, views)

---

## 17. 주문 처리 플로우

### 17.1 단일 주문
1. 사용자가 주문 생성
2. 포인트 차감
3. 즉시 SMM Panel API 호출
4. work_jobs에 상태 기록
5. 2분 후 상태 확인 (in_progress)
6. 24시간 후 상태 확인 (completed)

### 17.2 패키지 주문
1. 사용자가 패키지 주문 생성
2. 포인트 차감
3. work_jobs에 각 단계별 작업 생성
4. 첫 번째 단계 시작
5. 각 단계 완료 후 다음 단계 시작
6. 모든 단계 완료 시 주문 완료

### 17.3 예약 주문
1. 사용자가 예약 주문 생성
2. 포인트 차감
3. work_jobs에 예약 작업 생성
4. 예약 시간 도달 시 스케줄러가 처리
5. SMM Panel API 호출
6. 주문 상태 업데이트

### 17.4 분할 주문
1. 사용자가 분할 주문 생성
2. 포인트 차감
3. work_jobs에 각 날짜별 작업 생성
4. 각 날짜마다 스케줄러가 처리
5. 일일 발송량 계산
6. SMM Panel API 호출

---

## 18. 추천인 시스템 플로우

### 18.1 추천인 코드 사용
1. 사용자가 추천인 코드 입력
2. 코드 유효성 검증
3. referrals 테이블에 기록
4. 쿠폰 발급 (특정 상품용, 1회 사용)
5. 추천인에게 커미션 기록 (10%, accrued 상태)

### 18.2 커미션 지급
1. 추천인이 출금 요청
2. payout_requests 테이블에 기록
3. 관리자가 승인
4. commissions 상태를 paid_out으로 변경
5. payouts 테이블에 기록
6. 지갑에 커미션 추가 (선택사항)

---

## 19. 환경 변수

### 19.1 필수 환경 변수
- `DATABASE_URL`: PostgreSQL 연결 문자열
- `SMMPANEL_API_KEY`: SMM Panel API 키
- `ADMIN_TOKEN`: 관리자 인증 토큰
- `KCP_SITE_CD`: KCP 사이트 코드
- `KCP_SITE_KEY`: KCP 사이트 키
- `KCP_CERT_PATH`: KCP 인증서 경로

### 19.2 Firebase 설정
- `FIREBASE_API_KEY`: Firebase API 키
- `FIREBASE_AUTH_DOMAIN`: Firebase Auth 도메인
- `FIREBASE_PROJECT_ID`: Firebase 프로젝트 ID

---

## 20. 배포

### 20.1 Render 배포
- **플랫폼**: Render.com
- **런타임**: Python 3.12.8
- **빌드 명령**: Node.js 설치 → 프론트엔드 빌드 → Python 패키지 설치
- **실행 명령**: `gunicorn backend:app --bind 0.0.0.0:$PORT --workers 4 --timeout 120`

### 20.2 데이터베이스
- **프로덕션**: Supabase PostgreSQL
- **연결**: Connection Pooler 또는 Direct Connection
- **마이그레이션**: `migrate_database.py` 스크립트 사용

---

## 21. 보안

### 21.1 인증
- Firebase Auth (Google, Kakao)
- 관리자 토큰 (`X-Admin-Token` 헤더)
- CORS 설정

### 21.2 데이터 보호
- SQL 인젝션 방지 (파라미터화된 쿼리)
- 환경 변수로 민감 정보 관리
- HTTPS 강제 (프로덕션)

---

## 22. 모니터링 및 로깅

### 22.1 로깅
- 모든 API 요청 로깅
- 에러 상세 로깅 (traceback)
- 데이터베이스 작업 로깅

### 22.2 헬스 체크
- **엔드포인트**: `GET /api/health`
- **기능**: 서버 상태 확인

### 22.3 데이터베이스 테스트
- **엔드포인트**: `GET /api/test/db`
- **기능**: 데이터베이스 연결 테스트

---

이 문서는 SNS PMT 프로젝트의 모든 기능을 포함합니다. 각 기능은 백엔드 API와 프론트엔드 UI로 구현되어 있으며, 데이터베이스 스키마와 연동되어 작동합니다.

