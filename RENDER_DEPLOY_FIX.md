# Render 배포 오류 수정 가이드

## ❌ 발생한 오류

```
ModuleNotFoundError: 'your_application' 이라는 이름의 모듈이 없습니다.
==> 'gunicorn your_application.wsgi' 실행
```

## 🔧 해결 방법

Render 대시보드에서 **시작 명령어(Start Command)**를 수정해야 합니다.

### 1단계: Render 대시보드 접속
1. https://dashboard.render.com 접속
2. `snspmt` 프로젝트 선택

### 2단계: Settings 탭으로 이동
- 왼쪽 메뉴에서 **Settings** 탭 클릭

### 3단계: 시작 명령어 수정
1. **Start Command** 섹션 찾기
2. 현재 값: `gunicorn your_application.wsgi` (잘못됨)
3. 다음 값으로 변경:
   ```
   gunicorn backend:app --bind 0.0.0.0:$PORT --workers 4 --timeout 120
   ```
4. **Save Changes** 클릭

### 4단계: 재배포
- Render가 자동으로 재배포를 시작합니다
- 배포 완료까지 몇 분 소요

## ✅ 올바른 시작 명령어

```
gunicorn backend:app --bind 0.0.0.0:$PORT --workers 4 --timeout 120
```

**설명**:
- `backend:app`: `backend.py` 파일의 `app` 객체 사용
- `--bind 0.0.0.0:$PORT`: Render의 PORT 환경변수 사용
- `--workers 4`: 워커 프로세스 4개
- `--timeout 120`: 타임아웃 120초

## 🔍 확인 방법

배포 완료 후 로그에서 다음 메시지 확인:
```
✅ 환경 변수 검증 완료
🚀 Backend server starting on port 8000
```

## 📝 참고

- `render.yml` 파일도 수정되었습니다 (다음 배포 시 자동 적용)
- `Procfile`도 올바르게 설정되어 있습니다
- Render 대시보드 설정이 우선 적용됩니다

---

**지금 할 일**: Render 대시보드 → Settings → Start Command 수정

