# AWS 설정
aws_region = "us-east-1"

# 도메인 설정
domain_name = "yourdomain.com"

# API 키 (실제 값으로 변경)
smm_api_key = "your_smm_panel_api_key_here"

# 데이터베이스 URL (실제 값으로 변경)
database_url = "postgresql://username:password@host:port/database"

# 환경 설정
environment = "production"
