// Firebase 설정 (사용 중단됨)
// 이 파일은 더 이상 사용되지 않습니다.
// localStorage 기반 인증으로 전환되었습니다.

console.warn('Firebase config는 더 이상 사용되지 않습니다. localStorage 기반 인증을 사용하세요.');

// 더미 객체들 (호환성을 위해)
const app = null;
const analytics = null;
const auth = null;

export { app, analytics, auth };