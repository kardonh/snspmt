# 백엔드 전체 기능 가이드

## 📋 목차
1. [시스템 개요](#시스템-개요)
2. [인증 및 사용자 관리](#인증-및-사용자-관리)
3. [포인트 시스템](#포인트-시스템)
4. [주문 관리](#주문-관리)
5. [추천인 시스템](#추천인-시스템)
6. [쿠폰 시스템](#쿠폰-시스템)
7. [관리자 기능](#관리자-기능)
8. [SMM Panel 통합](#smm-panel-통합)
9. [블로그 시스템](#블로그-시스템)
10. [공지사항](#공지사항)
11. [스케줄러 및 크론 작업](#스케줄러-및-크론-작업)

---

## 시스템 개요

### 기술 스택
- **프레임워크**: Flask (Python)
- **데이터베이스**: PostgreSQL (Supabase) / SQLite (로컬 개발)
- **인증**: Firebase Auth (Google, Kakao)
- **결제**: KCP (한국사이버결제)
- **외부 API**: SMM Panel API

### 주요 기능
- 사용자 인증 및 관리
- 포인트 충전 및 결제
- 주문 생성 및 관리
- 추천인 시스템 및 커미션
- 쿠폰 발급 및 사용
- 관리자 대시보드
- 블로그 및 공지사항

---

## 인증 및 사용자 관리

### 1. 사용자 등록
- **엔드포인트**: `POST /api/register`
- **기능**: 새 사용자 등록, 지갑 자동 생성
- **요청**: `{ "user_id", "email", "username" }`
- **응답**: 사용자 정보 및 초기 포인트

### 2. 사용자 정보 조회
- **엔드포인트**: `GET /api/users/<user_id>`
- **기능**: 사용자 정보 조회 (자동 생성 지원)
- **응답**: 사용자 정보 (username, email, wallet balance)

### 3. Google 로그인
- **엔드포인트**: `POST /api/auth/google-login`
- **기능**: Google OAuth 로그인 처리
- **요청**: `{ "id_token" }`
- **응답**: 사용자 정보 및 토큰

### 4. Kakao 로그인
- **엔드포인트**: `POST /api/auth/kakao-login`
- **기능**: Kakao OAuth 로그인 처리
- **요청**: `{ "access_token" }`
- **응답**: 사용자 정보 및 토큰

### 5. 일반 로그인
- **엔드포인트**: `POST /api/auth/login`
- **기능**: 이메일/비밀번호 로그인
- **요청**: `{ "email", "password" }`

---

## 포인트 시스템

### 1. 포인트 조회
- **엔드포인트**: `GET /api/points?user_id=<user_id>`
- **기능**: 사용자 지갑 잔액 조회
- **응답**: `{ "points": 1000, "user_id": "..." }`

### 2. 포인트 구매 신청
- **엔드포인트**: `POST /api/points/purchase`
- **기능**: 포인트 구매 신청 (수동 승인 필요)
- **요청**: `{ "user_id", "amount", "price" }`
- **응답**: 구매 신청 ID

### 3. KCP 결제 시스템
#### 3-1. 거래 등록
- **엔드포인트**: `POST /api/points/purchase-kcp/register`
- **기능**: KCP 결제 거래 등록
- **요청**: `{ "user_id", "amount", "price", "good_name", "pay_method" }`
- **응답**: `{ "ordr_idxx", "approval_key", "pay_url" }`

#### 3-2. 결제창 폼 데이터
- **엔드포인트**: `POST /api/points/purchase-kcp/payment-form`
- **기능**: 결제창 호출을 위한 폼 데이터 생성

#### 3-3. 결제 인증 결과
- **엔드포인트**: `POST /api/points/purchase-kcp/return`
- **기능**: KCP에서 전달받은 인증결과 처리

#### 3-4. 결제 승인
- **엔드포인트**: `POST /api/points/purchase-kcp/approve`
- **기능**: 실제 결제 승인 요청
- **요청**: `{ "ordr_idxx", "enc_data", "enc_info", "tran_cd" }`

### 4. 포인트 차감
- **엔드포인트**: `POST /api/points/deduct`
- **기능**: 주문 시 포인트 차감
- **요청**: `{ "user_id", "amount", "description" }`

### 5. 포인트 구매 내역
- **엔드포인트**: `GET /api/points/purchase-history?user_id=<user_id>`
- **기능**: 사용자의 포인트 구매 내역 조회
- **응답**: 구매 내역 목록

---

## 주문 관리

### 1. 주문 생성
- **엔드포인트**: `POST /api/orders`
- **기능**: 새 주문 생성 (단일/패키지/예약/분할)
- **요청**: 
  ```json
  {
    "user_id": "string",
    "order_type": "single|package|scheduled|split",
    "items": [
      {
        "variant_id": 1,
        "link": "https://...",
        "quantity": 100,
        "unit_price": 1000
      }
    ],
    "scheduled_at": "2025-11-20T10:00:00" (예약 주문 시)
  }
  ```
- **처리 과정**:
  1. 포인트 잔액 확인
  2. 포인트 차감
  3. 주문 생성
  4. SMM Panel API 호출 (단일 주문)
  5. work_jobs 생성 (패키지/예약 주문)

### 2. 주문 목록 조회
- **엔드포인트**: `GET /api/orders?user_id=<user_id>`
- **기능**: 사용자의 주문 목록 조회
- **응답**: 주문 목록 (상태, 금액, 생성일 등)

### 3. 주문 상태 확인
- **엔드포인트**: `POST /api/orders/check-status`
- **기능**: SMM Panel에서 주문 상태 확인
- **요청**: `{ "order_id" }`

### 4. 주문 상태 업데이트
- **엔드포인트**: `PUT /api/orders/<order_id>/status`
- **기능**: 주문 상태 수동 업데이트
- **요청**: `{ "status": "completed|processing|cancelled" }`

### 5. 패키지 주문 처리 시작
- **엔드포인트**: `POST /api/orders/start-package-processing`
- **기능**: 패키지 주문의 첫 번째 단계 시작
- **요청**: `{ "order_id" }`

### 6. 패키지 진행 상황 조회
- **엔드포인트**: `GET /api/orders/<order_id>/package-progress`
- **기능**: 패키지 주문의 진행 상황 조회
- **응답**: 현재 단계, 완료된 단계 수 등

### 7. 예약 주문 생성
- **엔드포인트**: `POST /api/scheduled-orders`
- **기능**: 예약된 주문 생성
- **요청**: `{ "user_id", "service_id", "link", "quantity", "price", "scheduled_at" }`

### 8. 예약 주문 목록 (관리자)
- **엔드포인트**: `GET /api/admin/scheduled-orders`
- **기능**: 모든 예약 주문 목록 조회
- **인증**: 관리자 토큰 필요

---

## 추천인 시스템

### 1. 내 추천인 코드 조회
- **엔드포인트**: `GET /api/referral/my-codes?user_id=<user_id>`
- **기능**: 사용자의 추천인 코드 및 통계 조회
- **응답**: 
  ```json
  {
    "referral_code": "REF12345678",
    "total_commission": 50000,
    "usage_count": 10
  }
  ```

### 2. 추천인 코드 사용
- **엔드포인트**: `POST /api/referral/use-code`
- **기능**: 추천인 코드 사용 (쿠폰 발급)
- **요청**: `{ "user_id", "referral_code" }`
- **처리**:
  1. 추천인 코드 유효성 확인
  2. referrals 테이블에 기록
  3. 쿠폰 발급 (특정 상품용, 1회 사용)

### 3. 추천인 코드 검증
- **엔드포인트**: `GET /api/referral/validate-code?code=<code>`
- **기능**: 추천인 코드 유효성 검증
- **응답**: `{ "valid": true/false }`

### 4. 커미션 조회
- **엔드포인트**: `GET /api/referral/commissions?user_id=<user_id>`
- **기능**: 사용자의 커미션 내역 조회
- **응답**: 커미션 목록 (상태, 금액, 생성일)

### 5. 커미션 포인트 조회
- **엔드포인트**: `GET /api/referral/commission-points?user_id=<user_id>`
- **기능**: 미지급 커미션 포인트 합계 조회
- **응답**: `{ "total_commission": 50000 }`

### 6. 커미션 거래 내역
- **엔드포인트**: `GET /api/referral/commission-transactions?user_id=<user_id>`
- **기능**: 커미션 거래 내역 상세 조회

### 7. 추천인 통계
- **엔드포인트**: `GET /api/referral/stats?user_id=<user_id>`
- **기능**: 추천인 관련 통계 조회
- **응답**: 총 추천인 수, 총 커미션 등

### 8. 추천인 목록
- **엔드포인트**: `GET /api/referral/referrals?user_id=<user_id>`
- **기능**: 내가 추천한 사용자 목록 조회

### 9. 출금 요청
- **엔드포인트**: `POST /api/referral/withdrawal-request`
- **기능**: 커미션 출금 요청
- **요청**: 
  ```json
  {
    "user_id": "string",
    "amount": 10000,
    "bank_name": "string",
    "account_number": "string",
    "account_holder": "string"
  }
  ```

### 10. 쿠폰 발급 (추천인)
- **엔드포인트**: `POST /api/referral/issue-coupon`
- **기능**: 추천인 코드 사용 시 쿠폰 자동 발급
- **요청**: `{ "user_id", "referral_code" }`

---

## 쿠폰 시스템

### 1. 사용자 쿠폰 조회
- **엔드포인트**: `GET /api/user/coupons?user_id=<user_id>`
- **기능**: 사용 가능한 쿠폰 목록 조회
- **응답**: 
  ```json
  [
    {
      "coupon_code": "COUPON123",
      "coupon_name": "10% 할인",
      "discount_type": "percentage",
      "discount_value": 10,
      "status": "active",
      "valid_until": "2025-12-31"
    }
  ]
  ```

---

## 관리자 기능

### 1. 관리자 통계
- **엔드포인트**: `GET /api/admin/stats`
- **기능**: 전체 통계 조회
- **인증**: `X-Admin-Token` 헤더 필요
- **응답**: 
  ```json
  {
    "total_users": 1000,
    "total_orders": 5000,
    "total_revenue": 10000000,
    "total_points_purchased": 5000000
  }
  ```

### 2. 포인트 구매 내역 관리
- **조회**: `GET /api/admin/purchases`
- **수정**: `PUT /api/admin/purchases/<purchase_id>`
- **기능**: 포인트 구매 신청 승인/거절
- **요청**: `{ "status": "approved|rejected" }`

### 3. 사용자 목록
- **엔드포인트**: `GET /api/admin/users`
- **기능**: 전체 사용자 목록 조회
- **쿼리 파라미터**: `page`, `limit`, `search`

### 4. 거래 내역
- **엔드포인트**: `GET /api/admin/transactions`
- **기능**: 전체 거래 내역 조회
- **쿼리 파라미터**: `page`, `limit`, `status`

### 5. 추천인 관리
#### 5-1. 추천인 코드 목록
- **엔드포인트**: `GET /api/admin/referral/codes`
- **기능**: 모든 추천인 코드 목록 조회

#### 5-2. 추천인 목록
- **엔드포인트**: `GET /api/admin/referral/list`
- **기능**: 모든 추천인 관계 목록 조회

#### 5-3. 추천인 등록
- **엔드포인트**: `POST /api/admin/referral/register`
- **기능**: 관리자가 추천인 코드 수동 등록
- **요청**: `{ "user_email", "referral_code" }`

#### 5-4. 모든 추천인 코드 활성화
- **엔드포인트**: `POST /api/admin/referral/activate-all`
- **기능**: 모든 추천인 코드 자동 활성화

#### 5-5. 커미션 개요
- **엔드포인트**: `GET /api/admin/referral/commission-overview`
- **기능**: 전체 커미션 통계 조회

#### 5-6. 커미션 지급
- **엔드포인트**: `POST /api/admin/referral/pay-commission`
- **기능**: 커미션 수동 지급
- **요청**: `{ "user_id", "amount" }`

#### 5-7. 커미션 지급 내역
- **엔드포인트**: `GET /api/admin/referral/payment-history`
- **기능**: 커미션 지급 내역 조회

#### 5-8. 커미션 목록
- **엔드포인트**: `GET /api/admin/referral/commissions`
- **기능**: 모든 커미션 내역 조회

### 6. 출금 요청 관리
#### 6-1. 출금 요청 목록
- **엔드포인트**: `GET /api/admin/withdrawal-requests`
- **기능**: 모든 출금 요청 목록 조회

#### 6-2. 출금 처리
- **엔드포인트**: `POST /api/admin/process-withdrawal`
- **기능**: 출금 요청 승인/거절
- **요청**: `{ "request_id", "status": "approved|rejected" }`

### 7. 공지사항 관리
#### 7-1. 공지사항 목록
- **엔드포인트**: `GET /api/admin/notices`
- **기능**: 모든 공지사항 목록 조회

#### 7-2. 공지사항 생성
- **엔드포인트**: `POST /api/admin/notices`
- **요청**: `{ "title", "body", "is_active" }`

#### 7-3. 공지사항 수정
- **엔드포인트**: `PUT /api/admin/notices/<notice_id>`
- **요청**: `{ "title", "body", "is_active" }`

#### 7-4. 공지사항 삭제
- **엔드포인트**: `DELETE /api/admin/notices/<notice_id>`

### 8. 이미지 업로드
- **엔드포인트**: `POST /api/admin/upload-image`
- **기능**: 관리자용 이미지 업로드
- **Content-Type**: `multipart/form-data`
- **응답**: 업로드된 이미지 URL

### 9. 데이터베이스 마이그레이션
- **엔드포인트**: `POST /api/admin/migrate-database`
- **기능**: 데이터베이스 스키마 마이그레이션 실행

### 10. 패키지 주문 재처리
- **엔드포인트**: `POST /api/admin/reprocess-package-orders`
- **기능**: 멈춰있는 패키지 주문 재처리

---

## SMM Panel 통합

### 1. SMM Panel 서비스 목록
- **엔드포인트**: `GET /api/smm-panel/services`
- **기능**: SMM Panel에서 제공하는 서비스 목록 조회
- **응답**: 서비스 목록 (ID, 이름, 가격 등)

### 2. SMM Panel API 프록시
- **엔드포인트**: `POST /api/smm-panel`
- **기능**: SMM Panel API 호출 프록시
- **요청**: 
  ```json
  {
    "action": "services|balance|add|status|refill|cancel",
    "service": "service_id",
    "link": "https://...",
    "quantity": 100,
    "comments": "string"
  }
  ```

### 3. SMM Panel 테스트
- **엔드포인트**: `GET /api/smm-panel/test`
- **기능**: SMM Panel API 연결 테스트

### 4. 주문 상태 자동 확인
- **기능**: 백그라운드 스케줄러가 5분마다 주문 상태 확인
- **처리**: 
  1. `pending` 상태 주문 조회
  2. SMM Panel API로 상태 확인
  3. 상태 업데이트 (in_progress → completed)

---

## 블로그 시스템

### 1. 블로그 포스트 목록
- **엔드포인트**: `GET /api/blog/posts`
- **기능**: 블로그 포스트 목록 조회
- **쿼리 파라미터**: `page`, `limit`, `category`, `tag`

### 2. 블로그 포스트 상세
- **엔드포인트**: `GET /api/blog/posts/<post_id>`
- **기능**: 블로그 포스트 상세 조회

### 3. 블로그 카테고리 목록
- **엔드포인트**: `GET /api/blog/categories`
- **기능**: 블로그 카테고리 목록 조회

### 4. 블로그 태그 목록
- **엔드포인트**: `GET /api/blog/tags`
- **기능**: 블로그 태그 목록 조회

### 5. 블로그 포스트 생성 (관리자)
- **엔드포인트**: `POST /api/blog/posts`
- **요청**: `{ "title", "content", "category", "tags" }`

### 6. 블로그 포스트 수정 (관리자)
- **엔드포인트**: `PUT /api/blog/posts/<post_id>`
- **요청**: `{ "title", "content", "category", "tags" }`

### 7. 블로그 포스트 삭제 (관리자)
- **엔드포인트**: `DELETE /api/blog/posts/<post_id>`

---

## 공지사항

### 1. 활성 공지사항 조회
- **엔드포인트**: `GET /api/notices/active`
- **기능**: 활성화된 공지사항 목록 조회
- **응답**: 공지사항 목록 (최신순)

---

## 스케줄러 및 크론 작업

### 1. 예약 주문 처리
- **엔드포인트**: `POST /api/cron/process-scheduled-orders`
- **기능**: 예약된 주문들을 처리하는 크론 작업
- **처리 과정**:
  1. `scheduled_at` 시간이 지난 예약 주문 조회
  2. 실제 주문 생성
  3. SMM Panel API 호출
  4. 주문 상태 업데이트

### 2. 분할 발송 처리
- **엔드포인트**: `POST /api/cron/process-split-deliveries`
- **기능**: 분할 발송 주문들을 처리하는 크론 작업
- **처리 과정**:
  1. 분할 발송 주문 조회
  2. 각 일자별 발송 처리
  3. 주문 상태 업데이트

### 3. 백그라운드 스케줄러
- **기능**: 주기적으로 실행되는 작업
  - 예약 주문 처리 (1분마다)
  - SMM Panel 상태 확인 (5분마다)
  - 패키지 주문 단계 처리

---

## 데이터베이스 구조

### 주요 테이블
- **users**: 사용자 정보
- **wallets**: 지갑 정보 (잔액)
- **wallet_transactions**: 지갑 거래 내역
- **orders**: 주문 정보
- **order_items**: 주문 항목
- **work_jobs**: 비동기 작업 큐
- **referrals**: 추천인 관계
- **commissions**: 커미션 내역
- **payout_requests**: 출금 요청
- **payouts**: 출금 내역
- **coupons**: 쿠폰 정보
- **user_coupons**: 사용자 쿠폰
- **notices**: 공지사항
- **blogs**: 블로그 포스트

---

## 환경 변수

### 필수 환경 변수
- `DATABASE_URL`: 데이터베이스 연결 URL
- `SMMPANEL_API_KEY`: SMM Panel API 키
- `ADMIN_TOKEN`: 관리자 인증 토큰
- `KCP_SITE_CD`: KCP 사이트 코드
- `KCP_SITE_KEY`: KCP 사이트 키

### 선택적 환경 변수
- `PORT`: 서버 포트 (기본값: 8000)
- `FLASK_ENV`: Flask 환경 (development/production)

---

## 에러 처리

### 공통 에러 응답 형식
```json
{
  "error": "에러 메시지",
  "details": "상세 정보 (선택)"
}
```

### 주요 에러 코드
- `400`: 잘못된 요청
- `401`: 인증 실패
- `403`: 권한 없음
- `404`: 리소스 없음
- `500`: 서버 오류

---

## 성능 모니터링

### API 성능 로깅
- 모든 API 요청의 응답 시간 로깅
- 1초 이상 걸리는 요청 경고
- 5초 이상 걸리는 요청 심각 경고

### 데이터베이스 연결 관리
- PostgreSQL 연결 풀링
- 연결 실패 시 자동 재시도
- Supabase Pooler 및 Direct Connection 지원

---

## 보안

### 관리자 인증
- `X-Admin-Token` 헤더로 관리자 인증
- 환경 변수에서 토큰 관리

### 파일 업로드
- 허용된 파일 확장자만 업로드 가능
- 최대 파일 크기: 16MB
- 파일명 보안 처리

---

## 정적 파일 서빙

### SPA 라우팅
- 모든 경로를 `index.html`로 서빙
- `/api/*` 경로는 API로 처리
- `/assets/*` 경로는 정적 파일로 처리

### 정적 파일
- `dist/` 폴더에서 정적 파일 서빙
- 이미지, CSS, JS 파일 지원

---

이 문서는 백엔드의 모든 기능을 포함하고 있습니다. 각 엔드포인트의 상세한 구현은 `backend.py` 파일을 참조하세요.

