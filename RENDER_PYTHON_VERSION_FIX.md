# Render Python 버전 설정 가이드

## ❌ 현재 문제

Render가 여전히 Python 3.13을 사용하고 있어 `psycopg2-binary`와 호환성 문제가 발생합니다.

## ✅ 해결 방법

### 방법 1: Render 대시보드에서 직접 설정 (권장)

1. **Render 대시보드 접속**
   - https://dashboard.render.com
   - `snspmt` 프로젝트 선택

2. **Settings 탭으로 이동**
   - 왼쪽 메뉴에서 **Settings** 클릭

3. **Python Version 설정**
   - **Python Version** 섹션 찾기
   - 드롭다운에서 **3.12.8** 선택
   - 또는 **3.11.9** 선택 (더 안정적)

4. **Save Changes** 클릭

5. **재배포**
   - Render가 자동으로 재배포 시작
   - Python 3.12.8 또는 3.11.9로 빌드됨

### 방법 2: 환경변수로 설정

Render 대시보드 → **Environment** 탭:
- **Key**: `PYTHON_VERSION`
- **Value**: `3.12.8` 또는 `3.11.9`
- **Save Changes** 클릭

### 방법 3: psycopg (psycopg3)로 전환 (선택적)

Python 3.13을 계속 사용하려면 `psycopg2-binary` 대신 `psycopg`를 사용할 수 있습니다.

**주의**: 이 경우 `backend.py`의 모든 `psycopg2` 코드를 `psycopg`로 변경해야 합니다.

## 📋 권장 설정

**Python 3.12.8** 또는 **Python 3.11.9** 사용을 권장합니다.

- Python 3.12.8: 최신 기능, psycopg2-binary와 호환
- Python 3.11.9: 더 안정적, 검증된 호환성

---

**지금 할 일**: Render 대시보드 → Settings → Python Version을 **3.12.8** 또는 **3.11.9**로 변경

