# Supabase 비밀번호 확인 및 업데이트 가이드

## 현재 상황

`backend.py`에서 하드코딩된 비밀번호가 Supabase 데이터베이스와 일치하지 않아 "Tenant or user not found" 오류가 발생하고 있습니다.

## 해결 방법

### 1. Supabase 대시보드에서 비밀번호 확인

1. [Supabase 대시보드](https://supabase.com/dashboard) 접속
2. 프로젝트 `sociality` 선택
3. **Settings** → **Database** 메뉴로 이동
4. **Database password** 섹션 확인

### 2. 비밀번호 재설정 (필요한 경우)

비밀번호를 모르거나 확인할 수 없는 경우:

1. **Settings** → **Database** 메뉴
2. **Database password** 섹션에서 **Reset database password** 클릭
3. 새 비밀번호 생성 (안전하게 저장)
4. 새 비밀번호를 `backend.py`에 반영

### 3. backend.py 업데이트

`backend.py` 파일의 1742번째 줄 근처에서:

```python
# 현재 (1742번째 줄)
db_password_bytes = b'KARDONH0813!'  # TODO: 대시보드에서 확인한 정확한 비밀번호로 변경

# 수정 예시 (대시보드에서 확인한 새 비밀번호로 변경)
db_password_bytes = b'YOUR_NEW_PASSWORD_HERE'
```

### 4. 비밀번호 URL 인코딩

비밀번호에 특수문자가 포함된 경우 URL 인코딩 필요:

- `!` → `%21`
- `@` → `%40`
- `#` → `%23`
- 등등...

하지만 `psycopg2.connect()`에 직접 파라미터로 전달할 때는 **인코딩하지 않고** 원본 비밀번호를 사용합니다.

### 5. 테스트

비밀번호 업데이트 후:

```bash
python backend.py
```

성공 메시지:
```
✅ 환경 변수 검증 완료
🚀 SNS PMT 앱 시작 중...
✅ 데이터베이스 초기화 완료
```

## 참고

- 현재 하드코딩된 비밀번호: `KARDONH0813!`
- 프로젝트 참조: `gvtrizwkstaznrlloixi`
- 지역: `ap-southeast-2`

