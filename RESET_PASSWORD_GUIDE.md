# Supabase 비밀번호 재설정 가이드

## 단계별 안내

### 1. 비밀번호 재설정

1. Supabase 대시보드의 **Database Settings** 페이지에서
2. **Database password** 섹션의 **Reset database password** 버튼 클릭
3. 새 비밀번호 생성 (안전하게 저장하세요)

### 2. 새 비밀번호를 backend.py에 반영

비밀번호를 재설정한 후, 새 비밀번호를 알려주시면 `backend.py`에 자동으로 반영하겠습니다.

또는 직접 수정:
- `backend.py` 파일의 1743번째 줄 근처
- `db_password_bytes = b'kardonh0813!'` 부분을 새 비밀번호로 변경

### 3. 테스트

비밀번호 업데이트 후:

```bash
python backend.py
```

성공 메시지:
```
✅ 환경 변수 검증 완료
🚀 SNS PMT 앱 시작 중...
✅ 데이터베이스 초기화 완료
```

## 참고사항

- **Network Restrictions**: 현재 "Your database can be accessed by all IP addresses"로 설정되어 있어 로컬 연결에 문제가 없습니다.
- **Connection Pooling**: Pool Size 15, Max Client Connections 200으로 설정되어 있습니다.
- **SSL**: 필요시 SSL 인증서를 다운로드하여 사용할 수 있습니다.

## 중요

비밀번호를 재설정하면:
1. 기존 연결이 모두 끊어질 수 있습니다
2. 새 비밀번호로 모든 연결을 업데이트해야 합니다
3. Render 환경변수도 업데이트해야 합니다 (배포 시)

